#include <string.h>
#include "main.h"

/**
 * _strlen - calulate string length
 * @s:string pointer to calculate its length
 * Return: string length as int
 */

int _strlen(char *s)
{
	return (strlen(s));
}
