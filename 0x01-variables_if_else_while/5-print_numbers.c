#include <stdio.h>

/**
 * main - Entry point
 * Description: print all single digit numbders of base 10 from 0
 * Return: Always (0)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	};
	printf("\n");
	return (0);

}
