#include <ctype.h>
#include "main.h"

/**
 * _isalpha - check if char is alphabet.
 * @c: The character to check
 *
 * Return: On success 1.
 * On failure, 0 is returned.
 */

int _isalpha(int c)
{
	return (isalpha(c));
}
